version: '3'

services:
    postgres:
        image: postgres:10.7-alpine
        container_name: facia-tool-postgres
        ports:
            - 5432:5432
        environment:
            - POSTGRES_USER=facia-tool
            - POSTGRES_PASSWORD=facia-tool
            - POSTGRES_DB=facia-tool
        volumes:
            - postgres-data:/var/lib/postgresql/data

volumes:
    postgres-data:
        driver: local
